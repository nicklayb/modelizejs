/* eslint-env mocha */
/*import { expect } from 'firenpm/mochaccino'
import { sayHello } from '../lib'

describe('sayHello', () => {
  it('should say hello', () => {
    expect(sayHello()).toEqual('hello')
  })
})
*/
